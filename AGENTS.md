# AGENTS.md

This repo's working notes, current state, and **"pick up here on the Mac"** handoff
live in **[CLAUDE.md](./CLAUDE.md)** — read that first. It's the source of truth for
what's shipped, how to run/verify each piece, and the flagged spots to check.

Quick pointers:
- macOS app: `macos/README.md` + `macos/TTStation/` (v0.2.0, **never compiled on a Mac yet**).
- Agent/CLI: `crates/` (Rust workspace). Box screen: `box-panel/`.
- Design/reference docs: `docs/`.
- tt-toplike (remote-QuietBox monitoring) is a **separate, owner-managed repo**.
